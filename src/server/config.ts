import * as path from "path";

export const dataPath = "../../../data";
export const configFile = path.resolve(__dirname, dataPath, "config.json");
export const pageListFile = path.resolve(__dirname, dataPath, "page-list");
export const pageDirectory = path.resolve(__dirname, dataPath, "pages");
